# PBL 前端菜单与内容组织结构优化方案

## 一、当前结构分析

### 1.1 数据库层级关系
```
课程 (pbl_courses)
  └── 1:N → 学习单元 (pbl_units)
          ├── 1:N → 学习资料 (pbl_resources)
          └── 1:N → 任务 (pbl_tasks)
  └── 1:N → 项目 (pbl_projects)
```

### 1.2 当前前端结构问题

#### 学生端 (Layout.vue)
现有菜单：
- 学习首页 `/`
- 项目列表 `/projects`
- 任务列表 `/tasks`
- 学习进度 `/progress`
- 个人信息 `/profile`

**问题：**
1. ❌ 缺少**课程列表**入口（顶层容器）
2. ❌ "项目列表"和"任务列表"是平级菜单，但实际上：
   - 项目应该属于课程级别
   - 任务应该属于单元级别
3. ❌ 没有明确的"课程 → 单元 → 资料/任务"的导航路径
4. ❌ 学习路径不清晰，用户无法直观理解层级关系

#### 管理端 (AdminDashboard.vue)
现有菜单：
- 概览 `/admin`
- 课程管理 `/admin/courses`
- 学习单元 `/admin/units`
- 资料管理 `/admin/resources`

**问题：**
1. ❌ "学习单元"和"资料管理"是平级菜单，但实际上：
   - 单元属于课程
   - 资料属于单元
2. ❌ 缺少"任务管理"入口
3. ❌ 缺少"项目管理"入口
4. ❌ 层级关系不明确

---

## 二、优化方案

### 2.1 学生端菜单结构优化

#### 推荐的菜单结构

```
🏠 学习首页                    /
   ├── 展示：我的课程列表
   ├── 展示：进行中的项目
   └── 展示：待完成的任务

📚 我的课程                    /courses
   ├── 课程列表页面
   └── 点击进入 → 课程详情页  /course/:courseId
       ├── 课程介绍
       ├── 学习单元列表（按顺序）
       ├── 课程项目
       └── 点击单元 → 单元学习页 /unit/:unitId

📖 单元学习                    /unit/:unitId (单独页面，隐藏侧边栏)
   ├── 左侧：学习步骤列表
   │   ├── 视频资料
   │   ├── 文档资料
   │   ├── 实践任务
   │   └── 在线测验
   ├── 中间：内容展示区
   └── 右侧：AI 助手

🎯 我的项目                    /projects
   ├── 展示：与小组相关的项目
   └── 点击项目 → 项目详情页  /project/:projectId

📊 学习进度                    /progress
   ├── 展示：各课程进度
   ├── 展示：各单元完成情况
   └── 展示：任务完成统计

👤 个人信息                    /profile
```

#### 导航路径示例

**用户学习流程：**
```
首页 → 我的课程 → 课程详情 → 学习单元 → 单元学习
  ↓                                      ↓
查看项目                            完成资料/任务
  ↓                                      ↓
项目详情                            标记完成/提交
  ↓                                      ↓
团队协作                            解锁下一步
```

---

### 2.2 管理端菜单结构优化

#### 推荐的菜单结构

```
📊 概览                        /admin
   ├── 系统数据概览
   ├── 课程统计
   ├── 学生活跃度
   └── 快捷操作

📚 课程管理                    /admin/courses
   ├── 课程列表（CRUD）
   ├── 点击课程 → 课程详情    /admin/course/:courseId
   │   ├── 基本信息编辑
   │   ├── 学习单元管理（可排序）
   │   └── 项目管理

📖 单元管理                    /admin/units
   ├── 方式1：显示所有单元（带课程筛选）
   └── 方式2：仅在课程详情页管理单元（推荐）
       ├── 点击单元 → 单元详情  /admin/unit/:unitId
       │   ├── 基本信息编辑
       │   ├── 学习资料管理（视频、文档、链接）
       │   ├── 任务管理
       │   └── 学习路径设计

📁 资源库                      /admin/resources
   ├── 视频资源管理
   ├── 文档资源管理
   ├── 链接资源管理
   └── 资源统计

✅ 任务管理                    /admin/tasks
   ├── 任务列表（按单元分组）
   ├── 任务审核
   └── 学生提交查看

🎯 项目管理                    /admin/projects
   ├── 项目列表（按课程分组）
   ├── 项目进度监控
   └── 团队管理

👥 用户管理                    /admin/users
   ├── 学生管理
   ├── 教师管理
   └── 分组管理
```

---

## 三、实现步骤

### 3.1 学生端优化步骤

#### 步骤 1：新增"我的课程"菜单
**文件：** `frontend/src/router/index.js`

```javascript
{
  path: 'courses',
  name: 'MyCourses',
  component: () => import('../views/MyCourses.vue'),
  meta: { requiresAuth: true, title: '我的课程' }
},
{
  path: 'course/:courseId',
  name: 'CourseDetail',
  component: () => import('../views/CourseDetail.vue'),
  meta: { requiresAuth: true, title: '课程详情' }
}
```

**文件：** `frontend/src/views/Layout.vue`

```vue
<el-menu-item index="/courses" class="menu-item">
  <el-icon><Reading /></el-icon>
  <template #title>我的课程</template>
</el-menu-item>
```

#### 步骤 2：创建课程相关页面

**新建文件：**
- `frontend/src/views/MyCourses.vue` - 我的课程列表
- `frontend/src/views/CourseDetail.vue` - 课程详情页

#### 步骤 3：优化 Dashboard 首页
**文件：** `frontend/src/views/Dashboard.vue`

改进为：
- 顶部：欢迎横幅 + 学习统计
- 中部：我的课程快速入口（卡片式）
- 下部：进行中的项目 + 待完成任务

#### 步骤 4：调整面包屑导航
**文件：** `frontend/src/views/UnitLearning.vue`

```vue
<el-breadcrumb separator="/">
  <el-breadcrumb-item :to="{ path: '/' }">首页</el-breadcrumb-item>
  <el-breadcrumb-item :to="{ path: '/courses' }">我的课程</el-breadcrumb-item>
  <el-breadcrumb-item :to="{ path: `/course/${courseId}` }">{{ courseName }}</el-breadcrumb-item>
  <el-breadcrumb-item>{{ currentUnit.title }}</el-breadcrumb-item>
</el-breadcrumb>
```

---

### 3.2 管理端优化步骤

#### 步骤 1：重组菜单结构
**文件：** `frontend/src/views/AdminDashboard.vue`

```vue
<el-menu>
  <el-menu-item index="/admin">
    <el-icon><HomeFilled /></el-icon>
    <span>概览</span>
  </el-menu-item>
  
  <el-menu-item index="/admin/courses">
    <el-icon><Reading /></el-icon>
    <span>课程管理</span>
  </el-menu-item>
  
  <!-- 改为子菜单或独立管理页面 -->
  <el-sub-menu index="content">
    <template #title>
      <el-icon><FolderOpened /></el-icon>
      <span>内容管理</span>
    </template>
    <el-menu-item index="/admin/units">学习单元</el-menu-item>
    <el-menu-item index="/admin/resources">学习资料</el-menu-item>
    <el-menu-item index="/admin/tasks">任务管理</el-menu-item>
  </el-sub-menu>
  
  <el-menu-item index="/admin/projects">
    <el-icon><Memo /></el-icon>
    <span>项目管理</span>
  </el-menu-item>
  
  <el-menu-item index="/admin/users">
    <el-icon><UserFilled /></el-icon>
    <span>用户管理</span>
  </el-menu-item>
</el-menu>
```

#### 步骤 2：创建课程详情管理页
**新建文件：**
- `frontend/src/views/AdminCourseDetail.vue`
  - Tab 1: 基本信息
  - Tab 2: 学习单元（列表+排序）
  - Tab 3: 课程项目
  - Tab 4: 数据统计

#### 步骤 3：创建单元详情管理页
**新建文件：**
- `frontend/src/views/AdminUnitDetail.vue`
  - Tab 1: 基本信息
  - Tab 2: 学习资料（视频、文档、链接）
  - Tab 3: 任务列表
  - Tab 4: 学习路径设计（拖拽排序）

#### 步骤 4：优化面包屑导航
在所有管理页面添加清晰的面包屑：

```
概览 > 课程管理 > 智能家居AI助手项目 > 第一单元 > 资料管理
```

---

## 四、页面层级关系图

### 4.1 学生端页面层级

```
学习首页 (Dashboard)
│
├─ 我的课程 (MyCourses)
│   └─ 课程详情 (CourseDetail)
│       ├─ 单元学习 (UnitLearning) - 独立全屏页面
│       │   ├─ 视频学习
│       │   ├─ 文档阅读
│       │   ├─ 任务提交
│       │   └─ 在线测验
│       └─ 课程项目 (ProjectDetail)
│
├─ 我的项目 (Projects)
│   └─ 项目详情 (ProjectDetail)
│
├─ 学习进度 (Progress)
│
└─ 个人信息 (Profile)
```

### 4.2 管理端页面层级

```
管理概览 (AdminDashboard)
│
├─ 课程管理 (AdminCourses)
│   └─ 课程详情 (AdminCourseDetail)
│       ├─ 单元管理
│       │   └─ 单元详情 (AdminUnitDetail)
│       │       ├─ 资料管理
│       │       └─ 任务管理
│       └─ 项目管理
│
├─ 资源库 (AdminResources)
│
├─ 项目管理 (AdminProjects)
│
└─ 用户管理 (AdminUsers)
```

---

## 五、API 调整建议

### 5.1 学生端 API

```javascript
// 课程相关
GET  /api/student/courses              // 获取我的课程列表
GET  /api/student/courses/:id          // 获取课程详情（含单元列表）
GET  /api/student/courses/:id/units    // 获取课程的单元列表

// 单元相关
GET  /api/student/units/:id            // 获取单元详情（含资料和任务）
GET  /api/student/units/:id/resources  // 获取单元的学习资料
GET  /api/student/units/:id/tasks      // 获取单元的任务列表

// 任务相关
POST /api/student/tasks/:id/submit     // 提交任务
GET  /api/student/tasks/:id/progress   // 获取任务进度

// 项目相关
GET  /api/student/projects             // 获取我的项目列表
GET  /api/student/projects/:id         // 获取项目详情
```

### 5.2 管理端 API

```javascript
// 课程管理
GET    /api/admin/courses              // 课程列表
POST   /api/admin/courses              // 创建课程
GET    /api/admin/courses/:id          // 课程详情
PUT    /api/admin/courses/:id          // 更新课程
DELETE /api/admin/courses/:id          // 删除课程

// 单元管理
GET    /api/admin/courses/:courseId/units     // 获取课程的单元列表
POST   /api/admin/courses/:courseId/units     // 创建单元
PUT    /api/admin/units/:id                   // 更新单元
DELETE /api/admin/units/:id                   // 删除单元
PUT    /api/admin/courses/:courseId/units/sort // 单元排序

// 资料管理
GET    /api/admin/units/:unitId/resources     // 获取单元的资料列表
POST   /api/admin/units/:unitId/resources     // 创建资料
PUT    /api/admin/resources/:id               // 更新资料
DELETE /api/admin/resources/:id               // 删除资料

// 任务管理
GET    /api/admin/units/:unitId/tasks         // 获取单元的任务列表
POST   /api/admin/units/:unitId/tasks         // 创建任务
PUT    /api/admin/tasks/:id                   // 更新任务
DELETE /api/admin/tasks/:id                   // 删除任务

// 项目管理
GET    /api/admin/courses/:courseId/projects  // 获取课程的项目列表
POST   /api/admin/courses/:courseId/projects  // 创建项目
PUT    /api/admin/projects/:id                // 更新项目
DELETE /api/admin/projects/:id                // 删除项目
```

---

## 六、UI/UX 优化建议

### 6.1 课程列表页
- 使用卡片式布局
- 显示课程封面、标题、描述、进度条
- 显示课程状态：进行中、已完成、未开始
- 支持筛选和搜索

### 6.2 课程详情页
- 顶部：课程标题、描述、封面图
- 中部：单元列表（时间轴样式）
  - 显示单元标题、状态（已完成/进行中/锁定）
  - 显示进度条
  - 点击进入单元学习
- 底部：课程项目（卡片式）

### 6.3 单元学习页（已有）
- 保持现有的三栏布局
- 左侧：学习路径（资料+任务混合）
- 中间：内容展示
- 右侧：AI 助手

### 6.4 管理后台
- 使用表格+抽屉的方式管理内容
- 支持拖拽排序
- 实时预览功能
- 批量操作功能

---

## 七、总结

### 核心改进点

1. **明确层级关系**
   - 课程 → 单元 → 资料/任务
   - 课程 → 项目

2. **优化导航结构**
   - 学生端：首页 → 课程列表 → 课程详情 → 单元学习
   - 管理端：课程管理 → 课程详情 → 单元管理 → 资料/任务管理

3. **改善用户体验**
   - 清晰的面包屑导航
   - 直观的进度展示
   - 流畅的学习路径

4. **统一数据流向**
   - 前端结构与数据库层级一致
   - API 设计符合 RESTful 规范
   - 数据获取遵循层级关系

### 实施优先级

**P0 (必须):**
- ✅ 新增"我的课程"菜单和页面
- ✅ 创建课程详情页
- ✅ 优化单元学习页的面包屑导航

**P1 (重要):**
- 📝 调整管理端菜单结构
- 📝 创建课程和单元的详情管理页
- 📝 优化 Dashboard 首页

**P2 (可选):**
- 🔄 添加搜索和筛选功能
- 🔄 添加数据统计和可视化
- 🔄 完善权限控制
